package prototype.ex01;

/**
 * 프로토타입 패턴의 기본 인터페이스입니다.
 * 모든 프로토타입 가능한 객체가 구현해야 하는 복제 메서드를 정의합니다.
 */
public interface Prototype {
    /**
     * 현재 객체의 복제본을 생성하는 메소드입니다.
     * 이 메서드를 구현하는 클래스는 자신의 상태를 완전히 복사한 새로운 객체를 반환해야 합니다.
     * @return 현재 객체의 새로운 복제본
     */
    Prototype clone();
}
